development: &default
  KEY: value
  SMTP_ADDRESS:
  SMTP_PORT:
  SMTP_DOMAIN:
  SMTP_USER_NAME:
  SMTP_PASSWORD:
  SMTP_AUTHENICATION:
  SMTP_ENABLE_STARTTLS_AUTO:

test:
  <<: *default

production:
  <<: *default
